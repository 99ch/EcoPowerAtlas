export function AppFooter() {
  return (
    <footer className="app-footer">
      <small>
        Données servies par EcoPowerAtlas · Construite avec React + Vite · Responsive (grid + flex) · © {new Date().getFullYear()}
      </small>
    </footer>
  );
}
